---
interface Props {
  slot?: string;
  format?: 'auto' | 'fluid' | 'rectangle' | 'vertical' | 'horizontal';
  responsive?: boolean;
  className?: string;
  style?: string;
}

const { 
  slot = "7003918988204966", // Default slot or handle via props
  format = "auto",
  responsive = true,
  className = "",
  style = "display:block"
} = Astro.props;
---

<div class={`ad-container my-8 overflow-hidden rounded-xl bg-white/5 border border-white/10 p-4 transition-all hover:border-white/20 ${className}`}>
  <div class="text-[10px] uppercase tracking-wider text-white/30 mb-2 text-center">Advertisement</div>
  <ins class="adsbygoogle"
       style={style}
       data-ad-client={import.meta.env.PUBLIC_ADSENSE_ID}
       data-ad-slot={slot}
       data-ad-format={format}
       data-full-width-responsive={responsive ? "true" : "false"}></ins>
  <script is:inline>
       (adsbygoogle = window.adsbygoogle || []).push({});
  </script>
</div>

<style>
  .ad-container {
    min-height: 100px;
  }
</style>
