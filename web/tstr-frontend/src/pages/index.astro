---
import { supabase } from '../lib/supabase'
import Footer from '../components/Footer.astro'
import Header from '../components/Header.astro'

// Fetch all active listings with location data and category from database
const { data: listings, error } = await supabase
  .from('listings')
  .select(`
    *,
    category:category_id (
      id,
      name,
      slug
    ),
    location:location_id (
      id,
      name,
      slug,
      level,
      parent:parent_id (
        id,
        name,
        slug,
        level,
        parent:parent_id (
          id,
          name,
          slug,
          level
        )
      )
    )
  `)
  .eq('status', 'active')
  .order('created_at', { ascending: false })

// Log error if any
if (error) {
  console.error('Supabase error:', error)
}

// Fetch all categories from the database
const { data: allCategories, error: categoriesError} = await supabase
  .from('categories')
  .select('*')
  .order('name', { ascending: true });

// Log error if any
if (categoriesError) {
  console.error('Supabase categories error:', categoriesError);
}

// Extract countries from location hierarchy
const getCountryFromLocation = (location) => {
  if (!location) return null

  // If this is a country, return it
  if (location.level === 'country') {
    return { name: location.name, slug: location.slug }
  }

  // Navigate up parent chain to find country
  let current = location
  while (current && current.level !== 'country') {
    current = current.parent
  }

  return current ? { name: current.name, slug: current.slug } : null
}

const locations = listings ?
  [...new Map(
    listings
      .map(l => getCountryFromLocation(l.location))
      .filter(Boolean)
      .map(c => [c.slug, c])
  ).values()].slice(0, 8) : []

// Count stats
const totalListings = listings?.length || 0
const totalCategories = allCategories?.length || 0

// Prepare categories for display
const displayCategories = [];

if (allCategories && allCategories.length > 0) {
  // Combined Biopharma & Life Sciences category
  const biotechCat = allCategories.find(c => c.name === 'Biotech Testers');
  const pharmaCat = allCategories.find(c => c.name === 'Biopharma & Life Sciences Testers');
  const biotechCount = biotechCat && listings ? listings.filter(l => l.category_id === biotechCat.id).length : 0;
  const pharmaCount = pharmaCat && listings ? listings.filter(l => l.category_id === pharmaCat.id).length : 0;

  displayCategories.push({
    name: 'Biopharma & Life Sciences Testers',
    href: '/browse?category=Biopharma%20%26%20Life%20Sciences%20Testers',
    count: biotechCount + pharmaCount
  });

  // Add other categories
  allCategories
    .filter(cat => cat.name !== 'Biotech Testers' && cat.name !== 'Biopharma & Life Sciences Testers')
    .forEach(cat => {
      displayCategories.push({
        name: cat.name,
        href: `/browse?category=${encodeURIComponent(cat.name)}`,
        count: listings ? listings.filter(l => l.category_id === cat.id).length : 0
      });
    });
}
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TSTR Hub - Specialist Testing Services, Products and Solutions Directory</title>
  <meta name="description" content={`Find and compare ${totalListings} verified testing laboratories and certification bodies worldwide. ISO 17025 accredited labs for materials testing, environmental analysis, biotech, hydrogen infrastructure, and more. Get quotes from certified providers.`} />
  <meta name="keywords" content="testing laboratory, ISO 17025, certification body, materials testing, environmental testing, biotech testing, hydrogen testing, accredited labs, testing services" />
  <meta property="og:title" content="TSTR Hub - Specialist Testing Services Directory" />
  <meta property="og:description" content={`Directory of ${totalListings} verified testing laboratories and certification bodies. Compare ISO 17025 accredited labs worldwide.`} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://tstr.directory" />
  <script type="application/ld+json">
  {{
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "TSTR Hub",
    "description": "Specialist Testing Services, Products and Solutions Directory",
    "url": "https://tstr.directory",
    "potentialAction": {{
      "@type": "SearchAction",
      "target": "https://tstr.directory/browse?search={{search_term_string}}",
      "query-input": "required name=search_term_string"
    }}
  }}
  </script>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7003918988204966"
     crossorigin="anonymous"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #333;
      background: #f5f5f5;
    }
    

    
    main {
      max-width: 1200px;
      margin: 0 auto;
      padding: 3rem 2rem;
    }
    
    section {
      background: white;
      border-radius: 12px;
      padding: 2rem;
      margin-bottom: 2rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    
    h2 {
      font-size: 2rem;
      margin-bottom: 1.5rem;
      color: #667eea;
      border-bottom: 3px solid #667eea;
      padding-bottom: 0.5rem;
    }
    
    .category-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
      margin-top: 1.5rem;
    }
    
    .category-card {
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      padding: 1.5rem;
      transition: all 0.3s ease;
      cursor: pointer;
    }
    
    .category-card:hover {
      border-color: #667eea;
      transform: translateY(-4px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
    }
    
    .category-card h3 {
      color: #667eea;
      margin-bottom: 0.75rem;
      font-size: 1.25rem;
    }
    
    .category-card p {
      color: #666;
      font-size: 0.95rem;
      line-height: 1.5;
    }
    
    .location-list {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 1rem;
    }
    
    .location-tag {
      background: #f0f0f0;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.9rem;
      color: #555;
      transition: all 0.2s ease;
    }
    
    .location-tag:hover {
      background: #667eea;
      color: white;
      cursor: pointer;
    }
    
    footer {
      background: #333;
      color: white;
      text-align: center;
      padding: 2rem;
      margin-top: 3rem;
    }
    
    .cta-button {
      display: inline-block;
      background: #667eea;
      color: white;
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      margin-top: 1rem;
      transition: all 0.3s ease;
    }
    
    .cta-button:hover {
      background: #059669;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }
    
    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-top: 1.5rem;
    }
    
    .stat-card {
      background: linear-gradient(135deg, #667eea 0%, #059669 100%);
      color: white;
      padding: 1.5rem;
      border-radius: 8px;
      text-align: center;
    }
    
    .stat-number {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }
    
    .stat-label {
      font-size: 1rem;
      opacity: 0.9;
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .stat-description {
      font-size: 0.85rem;
      color: white;
      opacity: 0.9;
      line-height: 1.4;
      max-width: 220px;
      margin: 0 auto;
    }
  </style>
</head>
<body>
   <Header />

  <main>
    <!-- Value Propositions Section -->
    <section>
      <div class="stats">
        <div class="stat-card">
          <div class="stat-number">100%</div>
          <div class="stat-label">Free Access</div>
          <div class="stat-description">Browse all listings, no registration required</div>
        </div>
        <div class="stat-card">
          <div class="stat-number">100%</div>
          <div class="stat-label">URL Verified</div>
          <div class="stat-description">Every laboratory website confirmed active</div>
        </div>
        <div class="stat-card">
          <div class="stat-number">5+</div>
          <div class="stat-label">Niche Industries</div>
          <div class="stat-description">Oil & Gas, Biopharma & Life Sciences, Environmental, Materials</div>
        </div>
      </div>
    </section>

    <!-- Hydrogen Testing Spotlight -->
    <section style="background: linear-gradient(135deg, #0066cc 0%, #004d99 100%); color: white; padding: 3rem 2rem; border-radius: 12px; text-align: center;">
      <h2 style="color: white; font-size: 2.2rem; margin-bottom: 1rem;">⚡ Hydrogen Infrastructure Testers</h2>
      <p style="font-size: 1.2rem; margin-bottom: 1rem; opacity: 0.95;">
        Find labs certified for <strong>ISO 19880-3</strong> valve testing, <strong>700 bar pressure</strong>, and hydrogen embrittlement
      </p>
      <p style="font-size: 1rem; margin-bottom: 2rem; opacity: 0.9;">
        The only directory indexed by H2 standards: ISO 19880-3/5, SAE J2601, ISO 11114-4, UN ECE R134
      </p>
      <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
        <a href="/hydrogen-testing" style="background: white; color: #0066cc; padding: 1rem 2rem; border-radius: 8px; text-decoration: none; font-weight: 600; display: inline-block; transition: all 0.3s ease;">
          Explore H2 Labs →
        </a>
        <a href="/search/standards?filter=hydrogen" style="background: transparent; border: 2px solid white; color: white; padding: 1rem 2rem; border-radius: 8px; text-decoration: none; font-weight: 600; display: inline-block; transition: all 0.3s ease;">
          Search by Standard
        </a>
      </div>
    </section>

    <!-- International Coverage Section -->
    <section>
      <h2>International Coverage</h2>
      <p style="margin-bottom: 1rem; color: #666;">
        Testing service providers ready to serve you in select markets internationally
      </p>
      <div class="location-list">
        {locations.map((location) => (
          <a href={`/browse/${location.slug}`} class="location-tag" style="text-decoration: none; cursor: pointer;">{location.name}</a>
        ))}
      </div>
      {locations.length === 0 && (
        <p style="text-align: center; color: #666; margin-top: 1rem;">
          International coverage will expand as more providers are added.
        </p>
      )}
    </section>

    <!-- Browse Listings CTA -->
    <section style="text-align: center;">
      <h2>Browse Testers</h2>
      <p style="margin: 1rem 0 2rem; color: #666; font-size: 1.1rem;">
        Explore {totalListings} testing service providers worldwide
      </p>
      <a href="/browse" class="cta-button" style="display: inline-block; margin-right: 1rem;">View All Listings</a>
      <a href="/search/standards" class="cta-button" style="display: inline-block; background: #059669;">Search by Standard</a>
    </section>

    <!-- Categories Section -->
    <section>
      <h2>Browse by Category</h2>
      <div class="category-grid">
        {displayCategories.map((category) => (
          <a href={category.href} style="text-decoration: none; color: inherit;">
            <div class="category-card">
              <h3>{category.name}</h3>
              <p>{category.count} listings</p>
            </div>
          </a>
        ))}
      </div>
      {displayCategories.length === 0 && (
        <p style="text-align: center; color: #666; margin-top: 2rem;">
          Categories will appear as listings are added.
        </p>
      )}
    </section>

    <!-- CTA Section -->
    <section style="text-align: center; background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);">
      <h2 style="color: #333;">List Your Company</h2>
      <p style="margin: 1rem 0; color: #555; font-size: 1.1rem;">
        Join our directory and connect with clients seeking testing services
      </p>
      <a href="/submit" class="cta-button">Get Started Free</a>
    </section>
  </main>

  <Footer />
</body>
</html>
